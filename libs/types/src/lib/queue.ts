/**
 * Types for all the SSE-Events for the Queue
 */

export type matchFound = { id: string; opponent: string };

export type deletePlayer = { id: string };

export type addPlayer = { id: string; name: string };
